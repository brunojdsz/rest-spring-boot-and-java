version: '3.9'
services:
    db:
        image: mysql:8.0.29
        command: mysqld --default-authentication-plugin=mysql_native_password
        restart: always
        environment: 
            TZ: America/Sao_Paulo
            MYSQL_ROOT_PASSWORD: 1234
            MYSQL_USER: docker
            MYSQL_PASSWORD: 1234
            MYSQL_DATABASE: rest_spring_boot_brunojdsz
            MYSQL_ROOT_HOST: '%'
            MYSQL_TCP_PORT: 3308
        ports:
            - 3308:3308
        expose:
            - 3308
        networks:
            - bruno-network
    rest-spring-boot-and-java:
        image: brunojdsz/rest-spring-boot-and-java
        restart: always
        build: ./
        working_dir: /app
        environment:
            TZ: America/Sao_Paulo
            SPRING.DATASOURCE.URL: jdbc:mysql://db:3308/rest_spring_boot_brunojdsz?useTimezone=true&serverTimezone=UTC
            SPRING.DATASOURCE.USERNAME: root
            SPRING.DATASOURCE.PASSWORD: 1234
        ports:
            - 80:80
        command: mvn spring-boot:run
        depends_on:
            - db
        networks:
            - bruno-network
networks:
    bruno-network:
        driver: bridge